<!DOCTYPE html>
<html>
  <head>
    <title>8bitc.art</title>

    <meta
      name="viewport"
      content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width, viewport-fit=cover"
    />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= javascript_pack_tag "gallery" %>
    <%= stylesheet_link_tag "application", media: "all" %>
  </head>

  <body>
    <%= yield %>

    <script type="application/json" id="emulator"><%= asset_pack_path "emulator.js" %></script>
  </body>
</html>

